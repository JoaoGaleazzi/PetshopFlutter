name: petshop_project
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.7.0

dependencies:
  flutter:
    sdk: flutter
  shelf: ^1.4.0
  shelf_router: ^1.1.0
  mysql1: ^0.20.0
  crypto: ^3.0.3
  logging: ^1.2.0
  bcrypt: ^1.1.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
